\documentclass[
    tikz,
    border=5px,
    convert={
        density=600,
        size=400x400,
        outext=.svg,
        convertexe={dvisvgm},
        command=\unexpanded{{\convertexe\space --pdf  --zoom=1.5 \infile}}}]
    {standalone}
\usetikzlibrary{cd} % commutative diagrams
\usepackage{amsmath, amssymb, amsfonts, amscd}


\newcommand{\op}[1]{\operatorname{#1}}
\DeclareMathOperator{\Hom}{Hom}

\begin{document}
\begin{tikzcd}[sep=large]
    & {MA \times \Hom(A,MB)}
    \arrow[r,"\beta_{A,B}", red]
    & {MB}
    \arrow[dr,equal]
    && A \arrow[d, "f"]
    \\
    {MA \times \Hom(C,MB)}
    \arrow[ur,"{MA \times \Hom(f,MB)}"]
    \arrow[dr,"{Mf \times \Hom(C,MB)}"']
    &&& {MB}
    & C
    \\
    & {MC \times \Hom(C,MB)}
    \arrow[r,"\beta_{C,B}", red]
    & {MB}
    \arrow[ur,equal]
\end{tikzcd}
\end{document}